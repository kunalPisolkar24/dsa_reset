#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief You are given a 2D `grid` where each cell `(r, c)` has a non-negative integer cost. Find the path from `(0,0)` to `(n-1, m-1)` with the minimum sum of cell costs.
     *
     * @return int
     */
    int minCostGrid(const vector<vector<int>>& grid) {
        // Implementation details
        return {};
    }
};
