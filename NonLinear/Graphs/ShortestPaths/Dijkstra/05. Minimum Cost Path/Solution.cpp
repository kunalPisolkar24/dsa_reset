#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief Instead of just the distance, return the actual sequence of nodes (the path) that constitutes the shortest path from node `0` to node `n-1`.
     *
     * @return vector<int>
     */
    vector<int> shortestPath(int n, const vector<vector<int>>& edges) {
        // Implementation details
        return {};
    }
};
