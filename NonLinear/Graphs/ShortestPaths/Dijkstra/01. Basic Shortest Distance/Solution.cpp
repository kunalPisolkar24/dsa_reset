#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief Given a weighted undirected graph with `n` nodes and `edges` where each edge is `[u, v, w]`, find the shortest distance from node `0` to all other nodes.
     *
     * @return vector<int>
     */
    vector<int> shortestDistance(int n, const vector<vector<int>>& edges) {
        // Implementation details
        return {};
    }
};
