#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief In a graph where all edges have weight either 0 or 1, find the shortest distance from node `0` to all other nodes. This can be solved more efficiently than standard Dijkstra.
     *
     * @return vector<int>
     */
    vector<int> binaryShortestPath(int n, const vector<vector<int>>& edges) {
        // Implementation details
        return {};
    }
};
