#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief A signal starts at node `0` and travels through edges with weight `w` (representing time). Return the minimum time needed for the signal to reach EVERY node in the graph. If some nodes are unreachable, return -1.
     *
     * @return int
     */
    int signalTime(int n, const vector<vector<int>>& edges) {
        // Implementation details
        return {};
    }
};
