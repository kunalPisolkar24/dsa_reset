#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief You are given a weighted graph. Determine if there exists a path from node `0` to node `n-1` such that the total sum of edge weights on the path is less than or equal to `k`.
     *
     * @return bool
     */
    bool canReach(int n, const vector<vector<int>>& edges, int k) {
        // Implementation details
        return {};
    }
};
