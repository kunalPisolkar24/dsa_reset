#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief From node `0`, identify the node that has the maximum shortest-path distance. If multiple nodes share the same maximum distance, return the one with the smallest index.
     *
     * @return int
     */
    int farthestNode(int n, const vector<vector<int>>& edges) {
        // Implementation details
        return {};
    }
};
