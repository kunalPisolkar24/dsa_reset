#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    /**
     * @brief Calculate the number of different paths from node `0` to node `n-1` that all have the same minimum total weight. Return the count modulo 10^9+7.
     *
     * @return int
     */
    int countShortestPaths(int n, const vector<vector<int>>& edges) {
        // Implementation details
        return {};
    }
};
