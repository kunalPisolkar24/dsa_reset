#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int search(vector<int>& nums, int target) {
        // TODO: Implement binary search to return the index of target, or -1 if not present.
        return -1;
    }
};
