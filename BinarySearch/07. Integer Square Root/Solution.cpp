#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int mySqrt(int x) {
        // TODO: Return the largest integer y such that y*y <= x using binary search.
        // x is between 0 and 2^31 - 1.
        return 0;
    }
};
