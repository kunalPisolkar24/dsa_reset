#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    long long maxValueUnderConstraint(vector<int>& nums, long long budget) {
        // Find the maximum integer x such that sum(min(nums[i], x)) <= budget.
        // Use binary search on x.
        return 0;
    }
};
