#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    long long maxValidSquare(long long x) {
        // Find largest y such that y*y <= x.
        // Use binary search on y.
        return 0;
    }
};
