#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    long long minMaximumLoad(vector<int>& tasks, int k) {
        // Implement the "Binary Search on Answer Space" approach here.
        // Step 1: Define your range [low, high].
        // Step 2: Implement isPossible(maxLoad) to check if all tasks can be done with load <= mid.
        // Step 3: Run the binary search to find the minimum of such points.
        return 0;
    }
};
