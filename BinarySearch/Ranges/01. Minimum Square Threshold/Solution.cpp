#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    long long minSquareThreshold(long long x) {
        // Find smallest y such that y*y >= x.
        // Use binary search on y in range [1, x].
        return 0;
    }
};
