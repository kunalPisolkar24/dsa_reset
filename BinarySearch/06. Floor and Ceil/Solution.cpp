#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    pair<int, int> findFloorCeil(vector<int>& nums, int target) {
        // TODO: Return {floor, ceil}. 
        // Floor: largest element <= target. 
        // Ceil: smallest element >= target.
        // Return -1 if not found.
        return {-1, -1};
    }
};
