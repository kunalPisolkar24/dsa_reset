#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int findFirst(vector<int>& nums, int target) {
        // Helper to find the first occurrence
        return -1;
    }

    int findLast(vector<int>& nums, int target) {
        // Helper to find the last occurrence
        return -1;
    }

    int countFrequency(vector<int>& nums, int target) {
        // TODO: Use findFirst and findLast to compute total frequency of target.
        return 0;
    }
};
