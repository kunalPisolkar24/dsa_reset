#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        // TODO: Return the index where target should be inserted (first index i with nums[i] >= target).
        return -1;
    }
};
