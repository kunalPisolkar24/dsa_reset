#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<int> evenOddSort(vector<int> arr) {
        // Implement even numbers first (ascending), then odd numbers (descending)
        return {};
    }
};
