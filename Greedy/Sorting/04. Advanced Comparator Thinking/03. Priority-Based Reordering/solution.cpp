#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<int> priorityReorder(vector<int> arr, vector<int> priority) {
        // Implement reordering:
        // 1. Elements in 'priority' appear first (in same relative order)
        // 2. Remaining elements appear sorted ascending
        return {};
    }
};
