#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<string> multiLevelSort(vector<string> arr) {
        // Implement multi-level sort:
        // 1. Last character (ascending)
        // 2. Length (ascending)
        // 3. Lexicographical (ascending)
        return {};
    }
};
