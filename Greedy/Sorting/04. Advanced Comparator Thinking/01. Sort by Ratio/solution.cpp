#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<pair<int, int>> sortByRatio(vector<pair<int, int>> arr) {
        // Implement sort by ratio (value / weight) descending
        // Use cross-multiplication (v1 * w2 > v2 * w1) to avoid floating point errors
        // If ratios are equal, sort by value descending
        return {};
    }
};
