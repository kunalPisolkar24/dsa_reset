#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<pair<int, char>> stableSortByValue(vector<pair<int, char>> arr) {
        // Implement a stable sort by value (ascending)
        // Ensure relative order of labels for equal values is preserved
        return {};
    }
};
