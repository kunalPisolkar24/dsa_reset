#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<pair<int, int>> sortByStart(vector<pair<int, int>> intervals) {
        // Implement sort by start time (ascending)
        // If starts are equal, sort by end
        return {};
    }

    vector<pair<int, int>> sortByEnd(vector<pair<int, int>> intervals) {
        // Implement sort by end time (ascending)
        // If ends are equal, sort by start
        return {};
    }

    vector<pair<int, int>> sortByLength(vector<pair<int, int>> intervals) {
        // Implement sort by interval length (ascending)
        // If lengths are equal, sort by start
        return {};
    }
};
