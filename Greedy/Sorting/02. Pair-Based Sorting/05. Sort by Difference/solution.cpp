#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<pair<int, int>> sortByDifference(vector<pair<int, int>> arr) {
        // Implement sort by difference (a - b) ascending
        // If differences are equal, sort by first element (a) ascending
        return {};
    }
};
