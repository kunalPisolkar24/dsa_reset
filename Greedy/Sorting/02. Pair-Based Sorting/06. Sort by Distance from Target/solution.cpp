#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    vector<int> sortByDistance(vector<int> arr, int k) {
        // Implement sort by absolute distance from k
        // If distances are equal, smaller element first
        return {};
    }
};
