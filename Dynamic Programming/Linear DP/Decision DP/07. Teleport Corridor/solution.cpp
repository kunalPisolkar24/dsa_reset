#include <bits/stdc++.h>

using namespace std;

/**
 * Problem 7: Teleport Corridor
 * 
 * Start at index 0 (collect power[0]).
 * Move to i+1 or i+3.
 * Find max power.
 */
class Solution {
public:
    long long maxPower(vector<int>& power) {
        // Implement your solution here
        return 0;
    }
};
