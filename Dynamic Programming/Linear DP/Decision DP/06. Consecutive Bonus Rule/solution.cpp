#include <bits/stdc++.h>

using namespace std;

/**
 * Problem 6: Consecutive Bonus Rule
 * 
 * Choosing one element is allowed.
 * Choosing two consecutive elements is allowed.
 * Choosing THREE consecutive elements is NOT allowed.
 * Find max points.
 */
class Solution {
public:
    long long maxPoints(vector<int>& points) {
        // Implement your solution here
        return 0;
    }
};
