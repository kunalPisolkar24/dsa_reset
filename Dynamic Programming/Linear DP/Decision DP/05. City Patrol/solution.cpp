#include <bits/stdc++.h>

using namespace std;

/**
 * Problem 5: City Patrol
 * 
 * Cannot secure two sectors within distance 2.
 * If pick sector i, cannot pick i-1, i+1, i+2.
 * Find max risk eliminated.
 */
class Solution {
public:
    long long maxRisk(vector<int>& risk) {
        // Implement your solution here
        return 0;
    }
};
